{% trans_default_domain 'FOSUserBundle' %}



<div id="login-page" class="row">
    <div class="col s12 z-depth-4 card-panel">
        {#<div class="row">#}
        {##}
        {#</div>#}
        <div class="input-field col s12 center">
            <img src="{{ asset('assets/images/logo.png') }}" alt="" class="circle responsive-img valign profile-image-login">
            <p class="center login-form-text">El coso rojo</p>
        </div>
        {{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
        <div class="row">
            <div class="input-field col s12 center">
                <h4>Cambia tu contraseña</h4>
                {#<p class="center">Modifica</p>#}
            </div>
        </div>

        {{ form_widget(form) }}
        <div class="row">
            <div class="input-field col s12">
                <input class="btn red col s12" type="submit" value="{{ 'change_password.submit'|trans }}" />

            </div>

        </div>
        <div>
        </div>
        {{ form_end(form) }}
    </div>
</div>