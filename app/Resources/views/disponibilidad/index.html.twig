{#{% extends 'base.html.twig' %}#}
{% block stylesheets %}

    <link rel="stylesheet"  href="{{ asset('assets/css/materialize.css') }}" />
    <link rel="stylesheet"  href="{{ asset('assets/css/style.css') }}" />
    <link rel="stylesheet"  href="{{ asset('assets/css/prism.css') }}" />
    {#<link href="{{ asset('assets/css/page-center.css') }}" type="text/css" rel="stylesheet" media="screen,projection">#}


    <!-- INCLUDED PLUGIN CSS ON THIS PAGE -->
    <link href="{{ asset('assets/js/plugins/perfect-scrollbar/perfect-scrollbar.css') }}" type="text/css" rel="stylesheet" media="screen,projection">

{% endblock %}
{% block body %}
<body class="cyana">

        <div class="row">
            <div class="col s12 offset-m1 m6 ">
                {#<h1 class="center-align ">Crea tu negocio</h1>#}

                <div id="login-page" class="row">
                    <div class="col s12 offset-s4 z-depth-4  card-panel  ">

                        <div class="row">
                            <div class="input-field col s12 center">
                                <img src="{{ asset('assets/images/logo.png') }}" alt="" class="circle responsive-img valign profile-image-login">
                                <p class="center login-form-text">El coso rojo</p>
                                <p class="center login-form-text"></p>
                                <h6 >Lista de personas,canchas,etc. Y su tiempo de duracion</h6>

                            </div>
                        </div>
                        <div class="row">
                            <div class="input-field col s12 ">
                                <table>
                                    <thead>
                                        <tr>
                                            <th>Id</th>
                                            <th>Disinicioam</th>
                                            <th>Disfinam</th>
                                            <th>Disiniciopm</th>
                                            <th>Disfinpm</th>
                                            <th>Disdia</th>
                                            <th>Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                    {% for disponibilidad in disponibilidads %}
                                        <tr>
                                            <td><a href="{{ path('pers_disponibilidad_show', { 'id': disponibilidad.id }) }}">{{ disponibilidad.id }}</a></td>
                                            <td>{% if disponibilidad.disInicioAm %}{{ disponibilidad.disInicioAm|date('H:i:s') }}{% endif %}</td>
                                            <td>{% if disponibilidad.disFinAm %}{{ disponibilidad.disFinAm|date('H:i:s') }}{% endif %}</td>
                                            <td>{% if disponibilidad.disInicioPm %}{{ disponibilidad.disInicioPm|date('H:i:s') }}{% endif %}</td>
                                            <td>{% if disponibilidad.disFinPm %}{{ disponibilidad.disFinPm|date('H:i:s') }}{% endif %}</td>
                                            <td>{{ disponibilidad.disDia }}</td>
                                            <td>
                                                <ul>
                                                    <li>
                                                        <a href="{{ path('pers_disponibilidad_show', { 'id': disponibilidad.id }) }}">show</a>
                                                    </li>
                                                    <li>
                                                        <a href="{{ path('pers_disponibilidad_edit', { 'id': disponibilidad.id }) }}">edit</a>
                                                    </li>
                                                </ul>
                                            </td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                                <div class="row">
                                    <div class="input-field col s8 offset-s2">
                                        <a href="{{ path('pers_disponibilidad_new') }}">Create a new disponibilidad</a>

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </body>

    {#<ul>#}
        {#<li>#}
            {#<a href="{{ path('pers_disponibilidad_new') }}">Create a new disponibilidad</a>#}
        {#</li>#}
    {#</ul>#}
{% endblock %}
