{% extends 'base.html.twig' %}

{% block body %}
    <h1>Servicios list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Sernombre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for servicio in servicios %}
            <tr>
                <td><a href="{{ path('pers_servicio_show', { 'id': servicio.id }) }}">{{ servicio.id }}</a></td>
                <td>{{ servicio.serNombre }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('pers_servicio_show', { 'id': servicio.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('pers_servicio_edit', { 'id': servicio.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('pers_servicio_new') }}">Create a new servicio</a>
        </li>
    </ul>
{% endblock %}
