{% extends 'base.html.twig' %}

{% block body %}
    <h1>Personal</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personal.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_personal_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_personal_edit', { 'id': personal.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
